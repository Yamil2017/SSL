#include<stdlib.h>
#include"ListaEnlazada.h"

int main(){
	Nodo lista;
	Lista((10,&lista);
	for(int i=0;i<10;++i)
		set(&lista,i+3);
	for(int i=0;i<10;++1)
		printf("%d ",(lista+i).valor;
	return 0;
}
